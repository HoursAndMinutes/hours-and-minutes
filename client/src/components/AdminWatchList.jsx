const AdminWatchList = () => {
  //TO DO: Loop through all watches
  //Add edit button for each (put request)
  //Add delete button for each (delete request, are you sure?)
  return (
      <section>
        <p>
          (loop through all watches) for each watch, section with picture, id, name, edit details button, edit quantity button, delete button (delete button leads to are you sure?)
        </p>
      </section>
  )
}

export default AdminWatchList;